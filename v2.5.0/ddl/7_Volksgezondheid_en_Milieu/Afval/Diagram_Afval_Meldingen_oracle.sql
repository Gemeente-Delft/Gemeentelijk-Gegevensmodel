CREATE TABLE AANVRAAGOFMELDING (
    AANVRAAGOFMELDING_ID VARCHAR2(255) PRIMARY KEY,
        AFGEHANDELD NUMBER(1),
        CATEGORIE VARCHAR2(200),
        CATEGORIECODE VARCHAR2(80),
        DATUMAANMAAK DATE,
        DATUMAFHANDELING DATE,
        DATUMBEGINSTATUS DATE,
        DATUMEINDESTATUS DATE,
        HOOFDCATEGORIE VARCHAR2(80),
        HOOFDCATEGORIECODE VARCHAR2(80),
        IDENTIFICATIE VARCHAR2(255),
        KANAAL VARCHAR2(80),
        ONDERWERP VARCHAR2(200),
        ONDERWERPCODE VARCHAR2(80),
        SOORT VARCHAR2(80),
        STATUS VARCHAR2(80),
        STATUSCODE VARCHAR2(80),
        STATUSVOLGORDE VARCHAR2(80),
        SUBCATEGORIE VARCHAR2(200),
        SUBCATEGORIECODE VARCHAR2(80)
);

CREATE TABLE CATEGORIE (
    CATEGORIE_ID VARCHAR2(255) PRIMARY KEY,
        CODE NUMBER,
        NAAM VARCHAR2(80),
        OMSCHRIJVING CLOB
);

CREATE TABLE CONTAINERTYPE (
    CONTAINERTYPE_ID VARCHAR2(255) PRIMARY KEY,
        NAAM VARCHAR2(80),
        OMSCHRIJVING CLOB
);

CREATE TABLE FRACTIE (
    FRACTIE_ID VARCHAR2(255) PRIMARY KEY,
        NAAM VARCHAR2(80),
        OMSCHRIJVING CLOB
);

CREATE TABLE LOCATIE (
    LOCATIE_ID VARCHAR2(255) PRIMARY KEY,
        ADRESAANDUIDING VARCHAR2(255),
        LOCATIE VARCHAR2(255),
        LOCATIECODE VARCHAR2(255)
);

CREATE TABLE MELDING (
    AANVRAAGOFMELDING_ID VARCHAR2(255) PRIMARY KEY,
        24UURS NUMBER(1),
        BETREFT_CONTAINERTYPE_ID VARCHAR2(255),
        BETREFT_FRACTIE_ID VARCHAR2(255),
        BETREFT_LOCATIE_ID VARCHAR2(255),
        DATUMTIJD TIMESTAMP,
        HOOFDCATEGORIE_CATEGORIE_ID VARCHAR2(255),
        ILLEGAAL NUMBER(1),
        MELDINGNUMMER VARCHAR2(255),
        OMSCHRIJVING CLOB,
        SUBCATEGORIE_CATEGORIE_ID VARCHAR2(255)
);

ALTER TABLE MELDING ADD CONSTRAINT FK_MELDING_GEN_AANVRAAGOFMELDING FOREIGN KEY (AANVRAAGOFMELDING_ID) REFERENCES AANVRAAGOFMELDING (AANVRAAGOFMELDING_ID);
-- Generalization to AANVRAAGOFMELDING

ALTER TABLE MELDING ADD CONSTRAINT FK_MELDING_BETREFT FOREIGN KEY (BETREFT_LOCATIE_ID) REFERENCES LOCATIE (LOCATIE_ID);
-- BETREFT (0..* to 1, Connector_ID: 821)

ALTER TABLE MELDING ADD CONSTRAINT FK_MELDING_BETREFT_2 FOREIGN KEY (BETREFT_FRACTIE_ID) REFERENCES FRACTIE (FRACTIE_ID);
-- BETREFT (0..* to 0..1, Connector_ID: 827)

ALTER TABLE MELDING ADD CONSTRAINT FK_MELDING_BETREFT_3 FOREIGN KEY (BETREFT_CONTAINERTYPE_ID) REFERENCES CONTAINERTYPE (CONTAINERTYPE_ID);
-- BETREFT (0..* to 0..1, Connector_ID: 828)

ALTER TABLE MELDING ADD CONSTRAINT FK_MELDING_HOOFDCATEGORIE FOREIGN KEY (HOOFDCATEGORIE_CATEGORIE_ID) REFERENCES CATEGORIE (CATEGORIE_ID);
-- HOOFDCATEGORIE (0..* to 1, Connector_ID: 836)

ALTER TABLE MELDING ADD CONSTRAINT FK_MELDING_SUBCATEGORIE FOREIGN KEY (SUBCATEGORIE_CATEGORIE_ID) REFERENCES CATEGORIE (CATEGORIE_ID);
-- SUBCATEGORIE (0..* to 1, Connector_ID: 835)

