CREATE TABLE FUNCTIE (
    FUNCTIE_ID VARCHAR(255) PRIMARY KEY,
        CODE VARCHAR(50),
        NAAM VARCHAR(80),
        OMSCHRIJVING TEXT,
        SCHAAL VARCHAR(20),
        TAKEN TEXT
);

CREATE TABLE OPDRACHTGEVER (
    OPDRACHTGEVER_ID VARCHAR(255) PRIMARY KEY,
        CLUSTERCODE VARCHAR(20),
        CLUSTEROMSCHRIJVING VARCHAR(80),
        NAAM VARCHAR(80),
        NUMMER VARCHAR(20),
        OMSCHRIJVING TEXT,
        UITGEVOERD_DOOR_FUNCTIE_ID VARCHAR(255)
);

CREATE TABLE OPDRACHTNEMER (
    OPDRACHTNEMER_ID VARCHAR(255) PRIMARY KEY,
        CLUSTERCODE VARCHAR(20),
        CLUSTERCODEOMSCHRIJVING VARCHAR(80),
        NAAM VARCHAR(80),
        NUMMER VARCHAR(20),
        OMSCHRIJVING VARCHAR(80),
        UITGEVOERD_DOOR_FUNCTIE_ID VARCHAR(255)
);

ALTER TABLE OPDRACHTGEVER ADD CONSTRAINT FK_OPDRACHTGEVER_UITGEVOERD_DOOR FOREIGN KEY (UITGEVOERD_DOOR_FUNCTIE_ID) REFERENCES FUNCTIE (FUNCTIE_ID);
-- UITGEVOERD_DOOR (0..* to 1, Connector_ID: 479)

ALTER TABLE OPDRACHTNEMER ADD CONSTRAINT FK_OPDRACHTNEMER_UITGEVOERD_DOOR FOREIGN KEY (UITGEVOERD_DOOR_FUNCTIE_ID) REFERENCES FUNCTIE (FUNCTIE_ID);
-- UITGEVOERD_DOOR (0..* to 1, Connector_ID: 477)

