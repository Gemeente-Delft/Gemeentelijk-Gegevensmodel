CREATE TABLE STROOIDAG (
    STROOIDAG_ID VARCHAR(255) PRIMARY KEY,
        DATUM DATE,
        MAXIMUMTEMPERATUUR VARCHAR(255),
        MINIMUMTEMPERATUUR VARCHAR(255),
        TIJDMAXIMUMTEMPERATUUR TIMESTAMP,
        TIJDMINIMUMTEMPERATUUR TIMESTAMP
);

CREATE TABLE STROOIROUTE (
    STROOIROUTE_ID VARCHAR(255) PRIMARY KEY,
        ROUTE VARCHAR(255)
);

CREATE TABLE STROOIROUTEUITVOERING (
    STROOIROUTEUITVOERING_ID VARCHAR(255) PRIMARY KEY,
        GEPLANDEINDE DATE,
        GEPLANDSTART DATE,
        ROUTE VARCHAR(255),
        UITVOERING_OP_STROOIDAG_ID VARCHAR(255),
        VOLGENS_STROOIROUTE_ID VARCHAR(255),
        WERKELIJKEINDE DATE,
        WERKELIJKESTART DATE
);

ALTER TABLE STROOIROUTEUITVOERING ADD CONSTRAINT FK_STROOIROUTEUITVOERING_UITVOERING_OP FOREIGN KEY (UITVOERING_OP_STROOIDAG_ID) REFERENCES STROOIDAG (STROOIDAG_ID);
-- UITVOERING_OP (0..* to 0..1, Connector_ID: 1265)

ALTER TABLE STROOIROUTEUITVOERING ADD CONSTRAINT FK_STROOIROUTEUITVOERING_VOLGENS FOREIGN KEY (VOLGENS_STROOIROUTE_ID) REFERENCES STROOIROUTE (STROOIROUTE_ID);
-- VOLGENS (0..* to 1, Connector_ID: 1264)

