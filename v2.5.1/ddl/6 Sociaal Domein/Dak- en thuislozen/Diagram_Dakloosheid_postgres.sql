CREATE TABLE CLIENT (
    CLIENT_ID INTEGER PRIMARY KEY,
    CODE VARCHAR(80),
    GEZAGSDRAGERGEKEND BOOLEAN,
    JURIDISCHESTATUS VARCHAR(80),
    WETTELIJKEVERTEGENWOORDIGING VARCHAR(80)
);

CREATE TABLE DAKLOOSHEID (
    DAKLOOSHEID_ID INTEGER PRIMARY KEY,
    DATUMEIND DATE,
    DATUMSTART DATE,
    GEMEENTEOORSPRONG VARCHAR(200),
    HEEFT_CLIENT_ID INTEGER,
    TOESTEMMINGGEMEENTELIJKBRIEFADRES BOOLEAN,
    TOESTEMMINGNACHTOPVANG BOOLEAN
);

ALTER TABLE DAKLOOSHEID ADD CONSTRAINT FK_DAKLOOSHEID_HEEFT FOREIGN KEY (HEEFT_CLIENT_ID) REFERENCES CLIENT (CLIENT_ID);
-- HEEFT (1 to 0..*, Connector_ID: 1382)

