CREATE TABLE TENAAMSTELLING (
    TENAAMSTELLING_ID NUMBER PRIMARY KEY,
    AANDEELINRECHT VARCHAR2(255),
    BURGERLIJKESTAATTENTIJDEVANVERKRIJGING TIMESTAMP,
    DATUMBEGINGELDIGHEID DATE,
    DATUMEINDEGELDIGHEID DATE,
    EXPLOITANTCODE VARCHAR2(255),
    IDENTIFICATIETENAAMSTELLING VARCHAR2(255),
    VERKLARINGINZAKEDERDENBESCHERMING VARCHAR2(100),
    VERKREGENNAMENSSAMENWERKINGSVERBAND VARCHAR2(255)
);

CREATE TABLE AANTEKENING (
    AANTEKENING_ID NUMBER PRIMARY KEY,
    AARD VARCHAR2(100),
    BEGRENZING VARCHAR2(255),
    BETREFTGEDEELTEVANPERCEEL NUMBER(1),
    DATUMEINDE DATE,
    DATUMEINDERECHT DATE,
    IDENTIFICATIE VARCHAR2(100),
    OMSCHRIJVING VARCHAR2(200),
    TENAAMSTELLING_ID NUMBER
);

ALTER TABLE AANTEKENING ADD CONSTRAINT FK_AANTEKENING_TENAAMSTELLING FOREIGN KEY (TENAAMSTELLING_ID) REFERENCES TENAAMSTELLING (TENAAMSTELLING_ID);
-- unnamed (1 to 0..*, Connector_ID: 1981)

