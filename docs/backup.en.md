To make a backup of the DBMS — for example a copy for the test environment, or a backup file on disk — do the following:

Open the *Configure* tab and go to *Project Transfer*.

- Save to a file: **DBMS to File**
- Save to test environment: **DBMS to DBMS**. Select the production environment as *Source* and the test environment as *Target*.

Click *Transfer*.
