CREATE TABLE AANVRAAG (
    AANVRAAG_ID VARCHAR2(255) PRIMARY KEY,
        DATUMTIJD TIMESTAMP
);

CREATE TABLE ARCHIEFSTUK (
    ARCHIEFSTUK_ID VARCHAR2(255) PRIMARY KEY,
        BESCHRIJVING CLOB,
        INVENTARISNUMMER VARCHAR2(20),
        OMVANG VARCHAR2(255),
        OPENBAARHEIDSBEPERKING NUMBER(1),
        TREFWOORDEN VARCHAR2(200),
        UITERLIJKEVORM VARCHAR2(255)
);

CREATE TABLE BEZOEKER (
    BEZOEKER_ID VARCHAR2(255) PRIMARY KEY,
        DOET_AANVRAAG_ID VARCHAR2(255)
);

CREATE TABLE KP_ANVRG (
    KP_ANVRG_ID VARCHAR2(255) PRIMARY KEY,
    AANVRAAG_ID VARCHAR2(255),
    ARCHIEFSTUK_ID VARCHAR2(255)
);

ALTER TABLE KP_ANVRG ADD CONSTRAINT FK_KP_ANVRG_1 FOREIGN KEY (AANVRAAG_ID) REFERENCES AANVRAAG (AANVRAAG_ID);
-- AANVRAAG (many-to-many junction, Connector_ID: 1185)

ALTER TABLE KP_ANVRG ADD CONSTRAINT FK_KP_ANVRG_2 FOREIGN KEY (ARCHIEFSTUK_ID) REFERENCES ARCHIEFSTUK (ARCHIEFSTUK_ID);
-- ARCHIEFSTUK (many-to-many junction, Connector_ID: 1185)

ALTER TABLE BEZOEKER ADD CONSTRAINT FK_BEZOEKER_DOET FOREIGN KEY (DOET_AANVRAAG_ID) REFERENCES AANVRAAG (AANVRAAG_ID);
-- DOET (1 to 0..*, Connector_ID: 1184)

