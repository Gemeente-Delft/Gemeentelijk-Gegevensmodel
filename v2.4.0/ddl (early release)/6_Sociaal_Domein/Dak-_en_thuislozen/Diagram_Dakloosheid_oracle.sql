CREATE TABLE DAKLOOSHEID (
    DAKLOOSHEID_ID VARCHAR2(255) PRIMARY KEY,
        DATUMEIND DATE,
        DATUMSTART DATE,
        GEMEENTEOORSPRONG VARCHAR2(200),
        TOESTEMMINGGEMEENTELIJKBRIEFADRES NUMBER(1),
        TOESTEMMINGNACHTOPVANG NUMBER(1)
);

CREATE TABLE CLIENT (
    CLIENT_ID VARCHAR2(255) PRIMARY KEY,
        CODE VARCHAR2(80),
        GEZAGSDRAGERGEKEND NUMBER(1),
        HEEFT_DAKLOOSHEID_ID VARCHAR2(255),
        JURIDISCHESTATUS VARCHAR2(80),
        WETTELIJKEVERTEGENWOORDIGING VARCHAR2(80)
);

ALTER TABLE CLIENT ADD CONSTRAINT FK_CLIENT_HEEFT FOREIGN KEY (HEEFT_DAKLOOSHEID_ID) REFERENCES DAKLOOSHEID (DAKLOOSHEID_ID);
-- HEEFT (1 to 0..*, Connector_ID: 1382)

